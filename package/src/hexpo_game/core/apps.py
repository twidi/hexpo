"""App configuration for the hexpo_game.core app."""

from django.apps import AppConfig


class CoreConfig(AppConfig):
    """App configuration for the hexpo_game.core app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "hexpo_game.core"
